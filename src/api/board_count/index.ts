const router = require("express").Router()
const ctrl = require("./controller")

//조회수 증가
// router.post("/:board_id", ctrl.process.plus);
// //조회수 조회
// router.get("/:board_id", ctrl.process.read);


module.exports = router
export {}